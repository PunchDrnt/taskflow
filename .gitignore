# dependencies
node_modules/
.pnp.*

# yarn berry
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# builds
.next/
dist/
build/
out/
next-env.d.ts
.vercel

# turborepo
.turbo/

# env
# Everything but the examples: `.env` alone would not have covered
# .env.production, which holds the real database and Resend secrets.
.env*
!.env.example
!.env.production.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# testing
coverage/
.nyc_output/

# claude code
# skills + their tooling stay local for now - personal tooling, not part of the starter.
# .claude/CLAUDE.md is intentionally NOT ignored: it's the shared project instructions.
.claude/settings.local.json
.claude/skills/
skills-lock.json
.impeccable/

# editors / os
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.tsbuildinfo
