node_modules
.next
dist
build
.turbo
.yarn
pnp.*
*.tsbuildinfo

# vendored claude code skills - don't reformat third-party content
.claude/skills

# hand-aligned Thai markdown. Prettier pads table cells by code-point count,
# but Thai vowels and tone marks are zero-width combining characters, so it
# over-pads every cell containing them and the alignment comes out worse.
.claude/docs

yarn.lock
